# Michael Dominiecki Portfolio

Modern personal portfolio built with Next.js, React, Tailwind CSS, and Framer Motion.

## Run locally

```bash
pnpm install
pnpm dev
```

Open `http://localhost:3000`.

Inside the Codex desktop runtime, use:

```bash
pnpm dev:codex
```
